# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v54.0.0/ before writing any code.

# File Extensions Rule
Keep all the existing default `.tsx` files as they are. Any new files that are manually created should be in `.jsx` format.
